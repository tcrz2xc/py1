# Cloudflare Tunnel

Provides secure HTTPS access to internal services without port forwarding.

## Key Notes
- Requires cloudflared v19+
- Ingress rules must be unique
- Dashboard-published routes override config.yml
- Metrics available on port 8080
